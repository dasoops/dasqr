<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modules>
        <module>common</module>
        <module>cq-core-boot-starter</module>
        <module>plugin-core</module>
        <module>core</module>
        <module>cq-core-db-boot-starter</module>
        <module>plugin/plugin-gitNotice</module>
    </modules>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.7.4</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.dasoops.dasserver</groupId>
    <artifactId>dasServer</artifactId>
    <version>0.0.1</version>
    <name>parent</name>
    <packaging>pom</packaging>
    <description>dasServer parent</description>
    <properties>
        <java.version>11</java.version>
        <cq.version>0.0.1</cq.version>
        <mybatis-plus.version>3.5.2</mybatis-plus.version>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <lombok.version>1.18.24</lombok.version>
        <fastjson2.version>2.0.15</fastjson2.version>
        <fastjson2-extension.version>2.0.15</fastjson2-extension.version>
        <hutool.version>5.8.8</hutool.version>
        <log4j.version>1.2.17</log4j.version>
    </properties>


    <dependencyManagement>

        <dependencies>
            <!-- spring系列 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- 自定义组件 -->
            <dependency>
                <groupId>com.dasoops.dasserver</groupId>
                <artifactId>cq-core-boot-starter</artifactId>
                <version>${cq.version}</version>
            </dependency>

            <dependency>
                <groupId>com.dasoops.dasserver</groupId>
                <artifactId>cq-core-db-boot-starter</artifactId>
                <version>${cq.version}</version>
            </dependency>

            <dependency>
                <groupId>com.dasoops.dasserver</groupId>
                <artifactId>plugin-core</artifactId>
                <version>${cq.version}</version>
            </dependency>

            <dependency>
                <groupId>com.dasoops</groupId>
                <artifactId>common</artifactId>
                <version>${cq.version}</version>
            </dependency>

            <!-- DasServer plugin -->
            <dependency>
                <groupId>com.dasoops.dasserver</groupId>
                <artifactId>plugin-gitNotice</artifactId>
                <version>${cq.version}</version>
            </dependency>

            <!-- 通用组件 -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <scope>provided</scope>
                <optional>true</optional>
            </dependency>

            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson2.version}</version>
            </dependency>

            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2-extension</artifactId>
                <version>${fastjson2-extension.version}</version>
            </dependency>

            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

            <dependency>
                <groupId>log4j</groupId>
                <artifactId>log4j</artifactId>
                <version>${log4j.version}</version>
            </dependency>

        </dependencies>

    </dependencyManagement>

    <dependencies>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

</project>
